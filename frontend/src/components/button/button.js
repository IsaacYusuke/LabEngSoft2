import React from "react";
import "./button.css";

function Button(props) {
    const classes = "but " + props.className;

    return (
        <div className={classes}>
            
        </div>
    );
}

export default Button;